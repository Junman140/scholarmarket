services:
  mongodb:
    image: mongo:7
    container_name: scholarmarket-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: scholarmarket
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data:
